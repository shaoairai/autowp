# === Flask ===
SECRET_KEY=your-secret-key-here
FLASK_ENV=production

# === Database ===
DATABASE_URL=postgresql://autowp_user:your-db-password@postgres:5432/autowp_db
POSTGRES_DB=autowp_db
POSTGRES_USER=autowp_user
POSTGRES_PASSWORD=your-db-password

# === Admin Account ===
ADMIN_EMAIL=pig543879@gmail.com
ADMIN_PASSWORD=your-admin-password

# === Encryption ===
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-fernet-key-here

# === (The following are configured in the frontend UI, not needed in .env) ===
# Anthropic API Key -> frontend settings
# Gemini API Key -> frontend settings
# WordPress settings -> frontend settings
# SMTP settings -> frontend settings
